<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1280 640" width="1280" height="640" role="img" aria-label="moonlighter: The job-application assistant that leaves the last word to you.">
  <!-- The proof line below is an image, so scripts/check_readme_facts.py cannot read it.
       Keep the same NUMBERS as the facts blocks (the wording is shorter on purpose)
       and update it in the same commit whenever those numbers change. -->
  <rect width="1280" height="640" fill="#1F2B47"/>
  <g font-family="system-ui, -apple-system, Segoe UI, sans-serif" fill="#EEF1F6">
    <text x="96" y="250" font-size="72" font-weight="700">moonlighter</text>
    <rect x="96" y="275" width="220" height="6" fill="#E3A23B"/>
    <text x="96" y="330" font-size="40">The job-application assistant</text>
    <text x="96" y="385" font-size="40">that leaves the last word to you.</text>
    <text x="96" y="520" font-size="26" fill="#9AA8C4"><!-- facts -->1,700+ tests · 100% branch coverage · 5 packages on PyPI<!-- /facts --></text>
  </g>
</svg>
